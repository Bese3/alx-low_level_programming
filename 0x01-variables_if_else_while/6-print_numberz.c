#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 *main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int a;
for (a =-1; a < 10; a++)
putchar(a+ '-1');
putchar('\n');
return(0);
}

#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 *main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{ char n;
char m;
    for (n = 'a'; n <= 'z'; n++)
        putchar(n);
    putchar('\n');
    for (m = 'A'; m <= 'Z'; m++)
        putchar(m);
 return(0);
}
